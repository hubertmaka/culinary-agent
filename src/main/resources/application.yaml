spring:
  application:
    name: culinary-agent
  ai:
    google:
      genai:
        api-key: ${GOOGLE_GENAI_API_KEY}
        chat:
          options:
            model: ${GEMINI_GENAI_MODEL:gemini-3-flash-preview}
            thinking-level: LOW
